---
import PageLayout from '@/layouts/PageLayout.astro';
import Button from '@/components/ui/Button.astro';
---

<PageLayout title="404 | Página no encontrada" description="La página que buscas no existe o fue movida.">
  <section class="section-padding pt-32 sm:pt-36">
    <div class="container-custom">
      <div class="mx-auto max-w-2xl rounded-2xl border border-border bg-bg-secondary p-8 text-center sm:p-12" data-reveal>
        <p class="mb-2 text-xs font-semibold uppercase tracking-[0.2em] text-accent-primary">Error 404</p>
        <h1 class="font-heading text-5xl font-extrabold leading-tight text-text-primary sm:text-6xl">Página no encontrada</h1>
        <p class="mx-auto mt-4 max-w-xl text-text-secondary">
          La ruta que intentaste abrir no existe, cambió de URL o fue eliminada.
        </p>

        <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
          <Button text="Ir al inicio" href="/" size="lg" />
          <Button text="Ver proyectos" href="/proyectos" variant="outline" size="lg" />
        </div>
      </div>
    </div>
  </section>
</PageLayout>
